# Python build / cache artefacts
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/

# Test / tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Virtual environments
.venv/
venv/
env/

# MkDocs build output
site/

# Machine-local assistant settings (the shared .claude/settings.json, if one is
# ever added, stays tracked)
.claude/settings.local.json

# Local harvest output / Scopus keys
harvest*/
*.parquet
pybliometrics.cfg

# Stray R graphics output. Running the parity harness in this repository
# leaves an Rplots.pdf at the root when a plot is drawn with no device
# open; one was committed once and shipped inside the sdist.
Rplots.pdf
